# CodeToAGI — Episode 65: Environment Variables & Config Management

**Stop Hardcoding Secrets — Use .env + os.environ the Right Way**

## 🎯 What You’ll Learn

- Reading environment variables with `os.environ`
- Loading `.env` files using `python-dotenv`
- Secrets management best practices (`.gitignore`, `.env.example`)
- The 12-factor app pattern
- Building a robust `ConfigBot` that validates and fails fast

---

## 📁 Project Structure

```bash
ep65-env-config/
├── configbot_challenge_solution.py    # Final Challenge Solution
├── configbot_live.py                  # Live Build from Video
├── .env.example                       # Committed template
├── .gitignore
├── requirements.txt
├── README.md
